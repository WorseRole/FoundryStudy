# 阶段 2 串讲（Day 15）

> 对照 [知识地图 01 · Delegatecall / Proxy / Storage](/web3/恢复基础，建立知识地图)（知识库镜像）  
> Day 11–14：[Day11](./阶段2-Day11-Proxy原理.md) · [Day12](./阶段2-Day12-CounterV1V2.md) · [Day14 Sepolia](./阶段2-Day14-升级Sepolia.md)  
> 代码仓：[FoundryStudy](https://github.com/WorseRole/FoundryStudy)

---

## 1. 总句（阶段 2 一句话）

**用户只认 Proxy 地址；业务代码在 Implementation，经 delegatecall 执行，状态在 Proxy 的 storage；开业用 `ERC1967Proxy + initialize`，升级用 `upgradeToAndCall` 换 impl，不 new 第二个 Proxy。**

---

## 2. delegatecall 与 Proxy

- **是什么**：用 A 的 **代码**，改 **B 的 storage**。
- **UUPS 里**：A = Implementation，B = Proxy（用户调用的地址）。
- **常见误区**：对 **impl 地址** 调 `increment` 只改 impl 自己的小 storage，和用户的 Proxy **无关**。

---

## 3. 部署 vs 升级

| | Day 13 开业 | Day 14 升级 |
|--|-------------|-------------|
| 新 Proxy | ✅ `new ERC1967Proxy(v1, initData)` | ❌ 地址不变 |
| 新 impl | V1 | V2 |
| 关键调用 | `encodeCall(initialize, owner)` 在 constructor | `upgradeToAndCall(v2, data)` |
| 谁被调用 | 部署 Proxy 时内部 init | **owner 对 Proxy** 发交易 |

---

## 4. 四个 API（Day 13 必记）

| 名称 | 作用 |
|------|------|
| **ERC1967Proxy** | 门面合约：存 impl 指针 + fallback delegatecall |
| **abi.encodeCall(initialize, …)** | Proxy **创建时** 必须的 init calldata（v5 不宜空） |
| **__Ownable_init** | 在 **Proxy storage** 写 owner（替代 constructor） |
| **_disableInitializers** | impl 部署时锁 impl 本址，防抢 initialize |

---

## 5. UUPS 升级函数（Day 14 必记）

- **`upgradeToAndCall`** 写在 **Implementation（UUPSUpgradeable）** 里，不是 ERC1967Proxy 给用户的对外 API。
- 对 **Proxy 地址** 调 `upgradeToAndCall` → Proxy fallback → **delegatecall 进当前 impl** 的 UUPS 逻辑 → 改 ERC-1967 **implementation slot**。
- **`_authorizeUpgrade`**：内部钩子，常用 **`onlyOwner`**，防止任意人升级。
- **变砖**：若升级到 **没有 UUPS / 没有 upgradeToAndCall** 的 impl，Proxy **无法再换 impl**（业务可能还能跑）。

---

## 6. Storage Layout（Day 12–14）

- V2 与 V1 **已有变量顺序、类型不能改**，只能在 **末尾追加**（如 `bump`）。
- 乱序 → 升级后 **读错 slot**，不是简单的 deploy 失败。
- 新变量初值：升级时 **`reinitializer(2)` + `initializeV2`**（可放在 `upgradeToAndCall` 的 `data` 里，或升级后单独调）。

---

## 7. Transparent vs UUPS（Day 11）

| | Transparent | UUPS（本项目） |
|--|-------------|----------------|
| 升级写在哪 | Proxy Admin | Implementation |
| Proxy 厚度 | 较大 | 较薄 |

**业务项目**：MetaNode 子账户用 **EIP-1167 Clone**（多实例、省 gas），不是同址 UUPS 升级。

---

## 8. 升级风险（面试 3 条）

1. **Storage 冲突** — layout 不兼容。  
2. **初始化** — Proxy 开业必须 init；impl 要 `_disableInitializers`；升级用 `reinitializer` 勿重复 `initialize`。  
3. **权限 / UUPS** — 仅 owner 升级；新 impl 必须保留 UUPS。

---

## 9. 我的 Sepolia 流程

**Day 13（3 笔 tx）**

1. 部署 **CounterUpgradeableV1**（impl）  
2. 部署 **ERC1967Proxy**（constructor 内 init owner）  
3. 对 **Proxy** 调 **increment** → `number = 2`

**Day 14（2 笔 tx）**

1. 部署 **CounterUpgradeableV2**（impl）  
2. 对 **Proxy** 调 **upgradeToAndCall(v2, initializeV2(100))** → `number` 仍为 2，`bump = 100`，`version = "2"`

| 角色 | 地址 |
|------|------|
| **Proxy（用户只调这个）** | [0x901D9F0d66db49226476372e1B63684bFEbE4F73](https://sepolia.etherscan.io/address/0x901D9F0d66db49226476372e1B63684bFEbE4F73) |
| V1 impl | [0xC2fB0cB014D76fF0beDFE4CfaB8Aafba77036550](https://sepolia.etherscan.io/address/0xC2fB0cB014D76fF0beDFE4CfaB8Aafba77036550) |
| V2 impl | [0x22b7144CFd9D2A7e5936EB8418C2c33cf6dd6AC1](https://sepolia.etherscan.io/address/0x22b7144CFd9D2A7e5936EB8418C2c33cf6dd6AC1) |

---

## 10. 本地测试（Day 15 收口）

- `CounterUpgradeableProxyTest` — 开业 + increment  
- `CounterUpgradeableProxyTest2` — upgrade + bump + reset / version  
- `forge test` — **34 passed**

---

## 30 秒极简版（面试自测 · 口播稿）

我用 **UUPS + ERC1967Proxy**：用户只调 **Proxy 地址**，逻辑在 Implementation，**delegatecall** 改的是 **Proxy 上的 storage**。Day13 部署 V1 impl 和 Proxy，`initData` 里 **initialize** 设 owner，再 increment。Day14 只部署 V2 impl，**owner 对 Proxy 调 upgradeToAndCall**，`number` 不变，V2 有 reset 和 bump。注意 **storage 布局兼容**、**_authorizeUpgrade** 和 impl 上 **_disableInitializers**。

---

## 2 分钟口播版（可录音）

**（0:00）开场**  
阶段 2 做的是可升级 Counter。核心是 Proxy 模式：代码和状态分离，用户地址不变。

**（0:20）delegatecall**  
用户交易永远发给 Proxy。Proxy 用 delegatecall 执行 Implementation 里的函数，但 number、owner 存在 Proxy 的 slot 里。所以不能对 impl 地址做业务验收。

**（0:40）Day 13 开业**  
先 new V1 实现，再 new ERC1967Proxy，第二个参数是 encodeCall(initialize, owner)。Proxy 创建时就会 delegatecall initialize，在 Proxy 上写好 owner。Sepolia 上三笔交易：V1、Proxy、increment。

**（1:00）Day 14 升级**  
再 new V2 实现，但不要 new 第二个 Proxy。owner 对同一个 Proxy 调 upgradeToAndCall，把 implementation 指针从 V1 换成 V2。我带了 initializeV2(100) 给新变量 bump 赋值。链上 number 还是 2，说明 state 在 Proxy 上保留。

**（1:25）UUPS**  
upgradeToAndCall 定义在实现合约的 UUPSUpgradeable 里，不是 Proxy 壳上的对外 API。_authorizeUpgrade 用 onlyOwner 限制谁能升级。如果新 impl 删掉 UUPS，Proxy 就不能再升级，叫变砖。

**（1:45）风险与对比**  
升级要小心 storage layout，V2 不能打乱 V1 变量顺序。Transparent 把升级放在 Proxy Admin，UUPS 放在 Implementation。我们项目里子账户用的是 Clone，不是这种同址升级。

**（1:55）收尾**  
Sepolia Proxy 地址在 README 和 Etherscan 可查，本地 34 个测试全绿。阶段 2 完成。

---

## Day 15 Checklist

- [x] 本地 upgrade 测试（ProxyTest + ProxyTest2）
- [x] Sepolia Proxy / V1 / V2 记录
- [x] 本串讲 + 口播稿
- [ ] 自己录音练 1 遍 30 秒版（📝 自练）

**阶段 2 完成标准：** 能讲清 delegatecall、Proxy、升级注意事项 ✅

**下一步：** 阶段 3 · Day 16 MetaNode 永续
