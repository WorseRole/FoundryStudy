# 阶段 1 串讲（Day 10）

> 对照 [知识地图 01 · §9–15 交易生命周期](https://worserole.github.io/web3-learning-docs/web3/恢复基础，建立知识地图.html)  
> 链上命令：[知识库 · Forge/Cast 链上命令](https://worserole.github.io/web3-learning-docs/foundry/forge-cast-链上命令.html)  
> Sepolia 部署：[DEPLOY-流程.md](./DEPLOY-流程.md)

---

## 1. 交易生命周期（总句）

用户构造交易 → **本地私钥签名** → 经 **RPC（Alchemy）** 广播 → 节点校验后进 **Mempool** → 打进块 → **EVM 执行** → **改 State** → 出 **Receipt**（status、gasUsed、logs）。

---

## 2. calldata

- **是什么**：调用合约时交易里的 **`data`**，包含函数选择器 + 参数；纯转账可以 `data` 为空。
- **我的 Sepolia 例子**  
  - `cast send` 调 **`increment()`** / **`deposit()`** / **`withdraw()`**：`data` 里是函数编码；`deposit` 还带 **`value`（ETH）**。  
  - **`forge script` 部署**：`data` 是 **合约创建字节码**，Etherscan 上 To 显示 **Contract Created**。

---

## 3. 签名

- **是什么**：用 **私钥** 对交易签名，证明 **From** 是我。
- **我的例子**：`.env` 的 `PRIVATE_KEY`；与 MetaMask Sepolia 同一账户 **`0xE41280423a7b8ff6F9904e2CFc4C1E97f5506BeE`**；私钥不出本机，只把 **signed raw tx** 发给 RPC。

---

## 4. RPC

- **是什么**：通过 **`eth_sendRawTransaction`** 等 JSON-RPC 把交易交给节点，不直连验证者。
- **我的例子**：`.env` 的 **`SEPOLIA_RPC_URL`（Alchemy）**；命令里 **`--rpc-url sepolia`**。

---

## 5. nonce

- **是什么**：**我的 EOA（From 地址）** 上递增的交易序号，保证 **顺序 + 防重放**；不是 Counter/Vault 合约地址。
- **我的例子**：同一地址先 **部署 Counter** → **部署 Vault** → **`increment` / `deposit` / `withdraw`**，每笔占一个 nonce（0、1、2… 递增）。  
- **在哪里看**：Etherscan 打开 **我的地址** → 点某笔 tx → **Other Attributes → Nonce**；或  
  `cast nonce 0xE41280423a7b8ff6F9904e2CFc4C1E97f5506BeE --rpc-url sepolia`（下一笔该用的 nonce）。  
- **乱掉时**：nonce 太小（已用过）、跳号（中间缺一笔）、同一 nonce 两笔冲突、多端同时发 — 节点会拒或 pending 卡住。

---

## 6. gas

- **是什么**：EVM 执行消耗的费用 ≈ **gasUsed × gasPrice**。
- **我的例子**：部署 Counter 约 **34 万 gas**；`increment` 约 **4 万 gas**；Etherscan / `cast send` 回执里的 **Transaction Fee、gasUsed、status: 1**。  
- **对比**：**`cast call`** 只读 **不上链、不花 gas**；改 Storage（`number`、`balances`）更贵。

---

## 7. State 变化

- **Counter**：**`number`** 在 Storage，`increment` 后递增（代码里每次 +2）。  
- **Vault**：**`balances[地址]`** 与合约 ETH 余额；`deposit` / `withdraw` 后 **`cast call balances`** 可对上。

---

## 8. Receipt / Logs

- **Receipt**：`transactionHash`、`status`（1 成功）、`gasUsed`。  
- **Logs**：合约 **`emit` 事件** 才有；Counter/Vault 若未写 Event，**logs 为空是正常的**。

---

## 30 秒极简版（面试自测）

我用 Foundry 在 Sepolia 部署 Counter 和 Vault（`forge script --broadcast --verify`），再用 `cast send` 调 `increment`、`deposit`、`withdraw`；私钥在本地签名，经 Alchemy RPC 广播。节点检查 **nonce**、余额和签名，打包后 EVM 执行 **calldata** 里的函数，改 Storage 里的 `number` 和 `balances`，我付 **gas**，回执里看 **status** 和 **gasUsed**。只读状态用 **`cast call`**，不发交易、不花 gas。

---

## Sepolia 部署记录

| 合约 | 地址 |
|------|------|
| Counter | [0x00E60d96e3ccbe461700bEF3FC2B8b61EfAd2A1c](https://sepolia.etherscan.io/address/0x00E60d96e3ccbe461700bEF3FC2B8b61EfAd2A1c) |
| Vault | [0x605edB790b07dA3809E61ba24fbd4a29b9ad3D32](https://sepolia.etherscan.io/address/0x605edB790b07dA3809E61ba24fbd4a29b9ad3D32) |
